{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "devDependencies": {
    "concurrently": "^7.5.0",
    "tsx": "^3.6.0"
  },
  "scripts": {
    "dev": "npm run dev --prefix packages/server",
    "serve": "npm run serve --prefix packages/server",
    "clean": "npm run clean --prefix packages/server",
    "build": "npm run build --prefix packages/server",
    "release": "node packages/scripts/release.js"
  },
  "workspaces": [
    "packages/*"
  ]
}
