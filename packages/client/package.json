{
  "name": "@system-two/client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel watch ./src/*.html --dist-dir ../server/public",
    "build": "parcel build ./src/*.html --dist-dir ../server/public",
    "clean": "rm -rf ../server/public && rm -rf .parcel-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0-nightly.469"
  }
}
