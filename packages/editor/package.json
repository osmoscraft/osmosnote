{
  "name": "@system-two/editor",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "dev": "parcel ./src/*.html --dist-dir ../server/public",
    "build": "parcel build ./src/*.html --dist-dir ../server/public",
    "clean": "rm -rf ../server/public && rm -rf .parcel-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "nightly"
  }
}
